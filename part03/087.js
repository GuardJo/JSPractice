/**
 * Array.shift()
 * 배열의 첫 번째 요소 추출
 * 더 이상 추출할 요소가 없을 시 undefined 출력
 */
const arr = [1, 2, 3];
console.log(arr.shift());
console.log(arr.shift());
console.log(arr.shift());
console.log(arr.shift());